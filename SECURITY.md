# 安全策略

## 报告漏洞

Nexus 存的是用户各平台账号的明文凭证，任何能读到 `nexus.db`、能让网关把请求发错地方、
或能让一键接入改错配置文件的问题，都算安全问题。

**请不要在公开 issue 里报告。** 用 GitHub 的
[私密漏洞报告](https://github.com/roviix/nexus/security/advisories/new) 提交，附上：

- 影响的版本 / 提交
- 复现步骤或概念验证
- 你认为的影响范围

我们会在 7 天内确认收到，并在修复发布前与你保持沟通。修复后会在 Release 说明里致谢
（除非你不愿意）。

## 范围说明

下面这些是**已知的、有意为之的**设计取舍，不作为漏洞受理（理由见
[`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) §9.1 与 §11）：

- 凭证明文存在本地 SQLite，不进系统钥匙串；靠 `0700` / `0600` 权限与「一用户一机器」的假设保护。
- 网关只监听 `127.0.0.1`；同机其他进程能访问它是预期行为。
- 账号导出文件含明文凭证，应用会持续警告。

## 支持的版本

只有最新的 Release 会收到安全修复。
