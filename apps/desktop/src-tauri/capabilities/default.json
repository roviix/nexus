{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的权限。刻意开得很小：网络、文件、进程控制全在 Rust 侧，前端只需要能调 command 和收 event。opener 只放 http(s)/mailto 的默认范围，给「去网页控制台」这类外链用。set-theme 给外观设置用：把窗口（原生菜单、滚动条）的明暗跟网页同步。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-theme",
    "opener:default",
    "updater:default",
    "process:allow-restart"
  ]
}
